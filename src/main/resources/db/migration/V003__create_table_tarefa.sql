CREATE TABLE tarefa (
  id BIGINT GENERATED BY DEFAULT AS IDENTITY NOT NULL,
   titulo VARCHAR(255) NOT NULL,
   descricao VARCHAR(255) NOT NULL,
   prazo date NOT NULL,
   data_hora_inicio TIMESTAMP WITHOUT TIME ZONE NOT NULL,
   data_hora_fim TIMESTAMP WITHOUT TIME ZONE,
   duracao INTEGER NOT NULL,
   finalizado BOOLEAN NOT NULL,
   departamento_id BIGINT,
   pessoa_id BIGINT,
   CONSTRAINT pk_tarefa PRIMARY KEY (id)
);

ALTER TABLE tarefa ADD CONSTRAINT FK_TAREFA_ON_DEPARTAMENTO FOREIGN KEY (departamento_id) REFERENCES departamento (id);

ALTER TABLE tarefa ADD CONSTRAINT FK_TAREFA_ON_PESSOA FOREIGN KEY (pessoa_id) REFERENCES pessoa (id);